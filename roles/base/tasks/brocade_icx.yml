---
- name: Set Hostname
  community.network.icx_config:
    lines:
      - hostname {{ inventory_hostname }}

- name: Set Domain
  community.network.icx_config:
    lanes:
      - domain {{ domain_name }}

- name: Set DNS Servers
  community.network.icx_config:
    lanes:
      - dns-server {{ dns_server }}
