---
# tasks file for initial_cfg
- name: Arista EOS VLANs
  ansible.builtin.include_tasks: arista_eos/vlan.yml
  when:
    - ansible_network_os == 'arista.eos.eos'

# - name: Arista EOS VLAN L2
#   ansible.builtin.include_tasks: arista_eos/l2.yml
#   when:
#     - ansible_network_os == 'arista.eos.eos'

# - name: Arista EOS VLAN L3
#   ansible.builtin.include_tasks: arista_eos/l3.yml
#   when:
#     - ansible_network_os == 'arista.eos.eos'

- name: Brocade ICX VLANs
  ansible.builtin.include_tasks: brocade_icx/vlan.yml
  when:
    - ansible_network_os == 'community.network.icx'

# - name: Brocade ICX VLAN L2
#   ansible.builtin.include_tasks: brocade_icx/vlan.yml
#   when:
#     - ansible_network_os == 'community.network.icx'

# - name: Brocade ICX VLAN L3
#   ansible.builtin.include_tasks: brocade_icx/vlan.yml
#   when:
#     - ansible_network_os == 'community.network.icx'

- name: Juniper SRX VLANs
  ansible.builtin.include_tasks: juniper_junos/vlan.yml
  when:
    - ansible_network_os == 'junipernetworks.junos.junos'

# - name: Juniper SRX VLAN L2
#   ansible.builtin.include_tasks: juniper_junos/vlan.yml
#   when:
#     - ansible_network_os == 'junipernetworks.junos.junos'

# - name: Juniper SRX VLAN L3
#   ansible.builtin.include_tasks: juniper_junos/vlan.yml
#   when:
#     - ansible_network_os == 'junipernetworks.junos.junos'
